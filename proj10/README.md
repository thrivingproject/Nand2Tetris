# Jack Syntax Analyzer

Performs both tokenizing and parsing.

## Usage

`tsc && node/dist/JackAnalyzer.js source`

where *source* is either a file name of the form *Xxx*`.jack` or the name of a folder container one or more `.jack` files.
